// TODO: Separate configs by subject
let config = {
    'database_url': process.env.DATABASE_URL,
    'table_name': 'food_log',
    
    'repoName': 'nitai-bot',
    'userName': 'PUMATeam'
};

module.exports = config;